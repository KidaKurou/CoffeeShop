<div class="profile-block-class absolute top-28 right-3 bg-white overflow-hidden shadow rounded-lg border w-md max-w-md mx-0 hidden">
    <div class="flex items-center space-x-6 pl-10">
        <div class="relative w-16 h-16 overflow-hidden bg-gray-100 rounded-full dark:bg-gray-600">
            <svg class="absolute w-19 h-19 text-gray-400 -left-1" fill="currentColor" viewBox="0 0 20 20"
                xmlns="http://www.w3.org/2000/svg">
                <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd">
                </path>
            </svg>
        </div>
        <div class="px-4 py-5 sm:px-6">
            <h3 class="text-lg leading-6 font-medium text-gray-900">
                Профиль пользователя
            </h3>
            <p class="mt-1 max-w-2xl text-sm text-gray-500">
                Вот немного информации о пользователе.
            </p>
        </div>
    </div>
    <div class="border-t border-gray-200 px-4 py-5 sm:p-0">
        <dl class="sm:divide-y sm:divide-gray-200">
            <div class="py-3 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                <dt class="text-sm font-medium text-gray-500">
                    Полное имя
                </dt>
                <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
                    <%= user.name %>
                </dd>
            </div>
            <div class="py-3 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                <dt class="text-sm font-medium text-gray-500">
                    Email адрес
                </dt>
                <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
                    <%= user.email %>
                </dd>
            </div>
            <div class="py-3 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                <dt class="text-sm font-medium text-gray-500">
                    Телефон
                </dt>
                <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
                    <% if (user.phone) { %>
                        <%= user.phone %>
                    <% } else { %>
                        <span class="text-gray-400">Добавьте телефон</span>
                    <% } %>
                </dd>
            </div>
            <div class="py-3 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6">
                <dt class="text-sm font-medium text-gray-500">
                    Адрес
                </dt>
                <dd class="mt-1 text-sm text-gray-900 sm:mt-0 sm:col-span-2">
                    <% if (user.address) { %>
                        <%= user.address %>
                    <% } else { %>
                        <span class="text-gray-400">Добавьте адрес</span>
                    <% } %>
                </dd>
            </div>
            <div class="py-3 sm:py-5 sm:grid sm:grid-cols-3 sm:gap-4 sm:px-6 flex space-x-4">
                <a href="/shopping_cart"
                    class="block text-center flex-1 text-sm py-2 px-4 rounded-lg border border-gray-200 hover:bg-gray-100">
                    Корзина
                </a>
                <a href="/favorites"
                    class="block text-center flex-1 text-sm py-2 px-4 rounded-lg border border-gray-200 hover:bg-gray-100">
                    Изменить
                </a>
                <form action="/logout" method="POST">
                    <button
                        class="block text-center flex-1 text-sm py-2 px-4 rounded-lg border border-gray-200 hover:bg-gray-100">
                        Выйти
                    </button>
                </form>
            </div>
            <div class="py-3 sm:py-5 sm:px-6 space-x-4 hidden">
                <a href="/admin_panel"
                    class="block text-center flex-1 text-sm py-2 px-4 rounded-lg border border-gray-200 hover:bg-gray-100">
                    Админ панель
                </a>
            </div>
        </dl>
    </div>
</div>